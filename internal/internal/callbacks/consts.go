package callbacks

const (
	// AcceptCallback is the callback to accept an invitation.
	AcceptCallback string = "accept"
	// DeclineCallback is the callback to decline an invitation.
	DeclineCallback string = "decline"
)
